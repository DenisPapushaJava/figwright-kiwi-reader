{
  "name": "@figwright/kiwi-reader",
  "version": "0.0.0",
  "private": true,
  "description": "Read-only Figma scenegraph capture over the Kiwi browser wire protocol",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "fzstd": "^0.1.1",
    "kiwi-schema": "^0.5.0",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "tsdown": "^0.23.0"
  },
  "engines": {
    "node": "^24.11.0 || >=26.0.0"
  }
}
